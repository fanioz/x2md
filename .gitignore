# Auth files hold real credentials and must never be committed.
.x2md_auth.json
*.auth.json

# Generated Markdown output
*.md.out
out/

# Python bytecode
__pycache__/
*.pyc

# Vercel local state
.vercel/

# Apify local run storage (INPUT.json may hold private URLs; datasets hold scraped content)
storage/

# Node tooling (if ever added)
node_modules/

# Editor / session artifacts
.zcode/
.vercel

# CI workflow logs (apify.yml tee-s these into the repo root)
docker-build.log
apify-push.log
